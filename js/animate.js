
		$(window).scroll(function() {
			$('.header_top2').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('fadeInDown');
				}
			});
		});

		$(window).scroll(function() {
			$('.text2').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('fadeInRight');
				}
			});
		});

		$(window).scroll(function() {
			$('.left_text').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('bounceInLeft');
				}
			});
		});

		$(window).scroll(function() {
			$('.left_text2').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('bounceInLeft');
				}
			});
		});

		$(window).scroll(function() {
			$('.left_text3').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('bounceInLeft');
				}
			});
		});

		$(window).scroll(function() {
			$('.right_text').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('fadeInRight');
				}
			});
		});

		$(window).scroll(function() {
			$('.right_text2').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('fadeInRight');
				}
			});
		});

		$(window).scroll(function() {
			$('.right_text3').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('fadeInRight');
				}
			});
		});

		$(window).scroll(function() {
			$('.phone').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('fadeInUp');
				}
			});
		});


		$(window).scroll(function() {
			$('.article1').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('fadeInUp');
				}
			});
		});

		$(window).scroll(function() {
			$('.article2').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('fadeInRight');
				}
			});
		});


		$(window).scroll(function() {
			$('.allscreens').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('jello');
				}
			});
		});

		$(window).scroll(function() {
			$('.show_reel').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('zoomIn');
				}
			});
		});

		$(window).scroll(function() {
			$('.header_top4').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('fadeInDown');
				}
			});
		});

		$(window).scroll(function() {
			$('.allcontacts').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('zoomIn');
				}
			});
		});

		$(window).scroll(function() {
			$('.allnames').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+700) {
					$(this).addClass('fadeInUp');
				}
			});
		});

		$(window).scroll(function() {
			$('.quotes').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('zoomIn');
				}
			});
		});

		$(window).scroll(function() {
			$('.commentary').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('fadeInRight');
				}
			});
		});

		$(window).scroll(function() {
			$('.photo').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+700) {
					$(this).addClass('zoomIn');
				}
			});
		});

		$(window).scroll(function() {
			$('.name_comment').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+700) {
					$(this).addClass('slideInUp');
				}
			});
		});

		$(window).scroll(function() {
			$('.header_top5').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('slideInDown');
				}
			});
		});

		$(window).scroll(function() {
			$('.logotips').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+700) {
					$(this).addClass('slideInUp');
				}
			});
		});

		$(window).scroll(function() {
			$('.header_top7').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('fadeInUp');
				}
			});
		});

		$(window).scroll(function() {
			$('.input').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+700) {
					$(this).addClass('slideInLeft');
				}
			});
		});

		$(window).scroll(function() {
			$('.five_logos').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+700) {
					$(this).addClass('slideInUp');
				}
			});
		});

		$(window).scroll(function() {
			$('.phone5').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+700) {
					$(this).addClass('slideInRight');
				}
			});
		});

		$(window).scroll(function() {
			$('.footer_container').each(function() {
				var imagePos = $(this).offset().top;
				var topOfWindow = $(window).scrollTop();
				if (imagePos < topOfWindow+600) {
					$(this).addClass('slideInUp');
				}
			});
		});

		